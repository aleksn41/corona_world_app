<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/history_list_item_height"
    android:layout_marginLeft="@dimen/margin_medium"
    android:layout_marginRight="@dimen/margin_medium"
    android:orientation="vertical">

    <TextView
        android:id="@+id/itemTextView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:text="@string/default_elem_text"
        android:layout_gravity="center"
        android:background="@drawable/rectangle" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@id/itemTextView"
        android:layout_marginTop="@dimen/history_list_item_margin_image"
        android:layout_marginEnd="@dimen/margin_big"
        android:src="@drawable/ic_favourites_star24dp"
        android:contentDescription="@string/description_Star" />
</RelativeLayout>